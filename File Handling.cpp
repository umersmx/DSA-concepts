//FILE HANDLING
#include <fstream>
//keywords (used to open and access a file in code): 
//ofstream [creation + reading of the file]
//ifstream [reading only]
//keywords (general):
//noskipws [dont skip white spaces]
//built in function:
//eof(): End of file
//syntax: make an object
// ofstream obj("nameoffile.txt" :: ios.app) [creating a file]\
//WHhenever we access a file, closing it is a must through: filename.close();
//Database basic operations: CRUD (Create, Read, Update, Delete)
#include<iostream>
using namespace std;